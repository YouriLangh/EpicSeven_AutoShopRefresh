run pip install opencv-python numpy pytesseract mss pygetwindow pyautogui
or pip install -r requirements.txt

install 
.exe from https://github.com/UB-Mannheim/tesseract/wiki 
and run installer ( .exe)


Add it to your PATH:

Press Win + R → type sysdm.cpl → Advanced tab → Environment Variables.

Under System variables, edit Path, add:

C:\Program Files\Tesseract-OCR